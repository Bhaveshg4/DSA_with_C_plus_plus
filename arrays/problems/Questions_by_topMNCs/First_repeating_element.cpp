#include<iostream>
#include<algorithm>
#include<string>
using namespace std;
int main(){
   ///The task is to find the first repeating element in the array of integears ie an element that occurs more than once and whose index of first occurrence is smallest.
   
    
    return 0;
}